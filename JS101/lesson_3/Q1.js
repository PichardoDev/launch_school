/*function isDotSeparatedIpAddress(inputString) {

  let valid = false;
  let dotSeparatedWords = inputString.split(".");
  if (dotSeparatedWords.length === 4 && !dotSeparatedWords.find(word => { 
    return !isAnIpNumber(word)
  })){
      valid = true;
    }
    return valid;
  }
*/
console.log((undefined).toString());